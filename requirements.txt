# Core
google-genai>=1.0.0
asyncpg>=0.30.0        # PostgreSQL async driver (Phase 1; no-op when DATABASE_URL unset)
redis>=5.0.0           # async Redis client for session persistence (optional; no-op when REDIS_URL unset)
langgraph>=1.2.11
langchain-core>=1.5.2
langsmith>=0.11.1

# HTTP / tools
httpx>=0.28.1
tavily-python>=0.7.26
Pillow>=12.3.0

# Web inspector
streamlit>=1.62.0

# API server
fastapi>=0.141.1
uvicorn>=0.52.0
python-multipart>=0.0.32

# Validation
pydantic>=2.8.0

# Config
python-dotenv>=1.2.3

# Testing
pytest>=9.1.1
pytest-asyncio>=0.24.0
pytest-cov>=7.1.0

# Type checking
mypy>=1.11.0

# Linting / formatting
ruff==0.15.22
pre-commit>=4.6.2

# Security
pip-audit>=2.10.1

# macOS only — skipped automatically on Linux CI runners
rumps>=0.4.0; sys_platform == "darwin"
# opencv-python>=5.0.0; sys_platform == "darwin"  # webcam capture (mac_client.py)
# mss>=10.0.0; sys_platform == "darwin"            # screen capture (mac_client.py)
slowapi>=0.1.9

[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = --tb=short -q
markers =
    integration: end-to-end tests that call real APIs (need GOOGLE_API_KEY + TEST_IMAGE_PATH)
